# 恩惠/奖励数据标准化修改备忘录

## 变更记录 (2026-04-04)

### 1. 数据模块化拆分
将以下第三方集合文件中的恩惠、咒诅、契约及命运之线拆分为独立的 JSON 文件：
- `恩惠-obojimatallgrass.json` (10项)
- `恩惠-taldoreireborn.json` (3项)
- `黑暗契约-thecrookedmoon.json` (26项，包括黑暗契约与命运之线)

### 2. 目录结构标准化
根据 5etools 标准及项目数据架构进行重新分配：

#### 01_祝福与咒符 (Blessings & Charms)
- 移入 `fortune_s_grace-taldoreireborn.json`。

#### 05_咒诅与特殊 (Curses & Special)
- 移入 `curse_of_ruin-taldoreireborn.json`。
- 移入 `curse_of_strife-taldoreireborn.json`。

#### 08_设定特性 (Setting Features)
- **Obojima\恩惠**: 存放 10 项来自 `ObojimaTallGrass` 的恩惠 (Boon)。
- **TheCrookedMoon\命运之线**: 存放 13 项来自 `CrookedMoon24` 的命运之线 (Thread of Fate)。

#### 10_黑暗赠礼 (Dark Gifts)
- 整合了来自 `CrookedMoon24` 的 13 项**黑暗契约 (Dark Bargain)**，因其机制与黑暗赠礼高度相似。

### 3. 数据完整性与规范化 (Lossless Handling)
- **无损处理**: 严格遵循 `dnd-5e-lossless-data-handler` 准则，保留了所有 `entries` 中的文本、层级结构及内联标签（如 `{@dice}`, `{@variantrule}`, `{@condition}` 等）。
- **图文整合**: 将原本分离在 `rewardFluff` 中的插图信息直接整合进对应的 JSON 对象中（通过 `images` 或 `foundryImg` 字段），确保 UI 渲染时能直接获取配套图片。
- **2024规则对齐**: *The Crooked Moon* 的内容已确认采用 2024 (XPHB) 标签体系，文件名统一使用 `-crookedmoon24.json` 后缀。

### 4. 代码格式与数据一致性审计
- **全量去重校验**: 编写脚本遍历了 `08_恩惠` 下所有 309 个文件，验证出没有任何基于名称+来源层面的重复条目冲突。
- **文件结构标准化**: 将全量单对象型 JSON 文件重新封装为标准数组型 `[...]`，以符合 5etools 主体的数据摄取规范，并全部执行了 2-space 缩进的标准化处理机制。
- **孤儿条目修复**: 对 `06_背景特性` 中的特性分配了缺失的 `type: "Background Feature"`。
- 所有数据类型映射、归属判断均准确对应所在分类文件夹（例如 SG 落在超自然赠礼、Dark Bargain 归位暗黑赠礼）。

### 5. 文件命名约定
- 遵循 `[snake_case_english_name]-[source].json` 格式。
- 对于 2024 规则内容增加 `24` 标识。

---
**所有原始集合文件已在确认提取无误后被移除。数据已通过语法校验，且在 `08_恩惠` 文件夹内完成了去重核对（Tal'Dorei 与 Obojima 的内容均为该来源下唯一存在）。**

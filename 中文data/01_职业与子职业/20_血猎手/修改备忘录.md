# 20_血猎手 模块化与无损修复工程 - 修改备忘录

## 1. 核心架构重构
- **主类文件标准化**：重构了 `01_主类/血猎手-2024.json`。建立了完整的 `_meta` 信息，并将职业特性从 class 对象分离至 `classFeature` 数组。
- **全特性注入**：注入了 1-20 级所有 15 个核心职业特性的规则正文（如 `鲜血秘法`、`猩红仪式`、`惩戒烙印` 等），确保 100% 字面无损。
- **子职业 1-Way Binding**：所有子职业均采用 `subclass` + `subclassFeature` 引用模式，通过主类的 `subclasses` 列表实现动态链接，确保 Character Builder 正确加载。

## 2. 可选特性库建立 (Optional Features)
- **血猎手特性数据库**：创建了 `03_可选特性/血猎手特性.json`，完整录入了：
  - **14 种血咒 (Blood Curses)**：包含所有等级门槛和子职业专属血咒（如化狼的怒号、弑灵的驱魔）。
  - **21 种诱变剂 (Mutagens)**：完整保留了提升属性及特殊感官的炼金规则。
  - **7 种猩红仪式 (Crimson Rites)**：包含破晓、死亡等高级仪式。
- **门槛校验**：为所有特性配置了精确的 `prerequisite`（等级、职业、子职业限制），保证构建器自动筛选逻辑的准确性。

## 3. 子职业深度修复
- **弑灵结社**：整合了 `破晓血仪` 与 `升腾走` 等位面穿梭规则。
- **化狼结社**：详细录入了 `混种变形` 状态下的全部 4 项加成（野性之力、坚韧毛皮、掠食打击、嗜血）。
- **突变结社**：修复了 `诱变剂` 的配制与消耗逻辑文本，并关联了特性库中的 MTGN 实体。
- **渎魂结社**：建立了完整的 `契约魔法 (Pact Magic)` 施法进度表（戏法、法术位、环阶），并录入了 `异界同调` 的全生物类型（异怪、天界、元素、妖精、邪魔、亡灵）增益。

## 4. 目录说明
- `01_主类/血猎手-2024.json`: 主类数据、核心特性、背景文本、子职业索引。
- `02_子职业/`: 4 个结社原型文件，包含各自专属特性规则。
- `03_可选特性/血猎手特性.json`: 全量血咒、诱变剂、猩红仪式库。

> [!IMPORTANT]
> 本次修改严格执行 `dnd-5e-lossless-data-handler` 准则，实现了对 `SnowWolf; 血猎手 (2024).json` 的 1:1 高保真还原。
